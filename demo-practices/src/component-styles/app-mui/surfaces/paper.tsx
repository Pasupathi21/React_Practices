import { makeStyles} from '@material-ui/core'

export const paperMainStyle = makeStyles({
    container: {
        width: '98%',
        height: '100vh',
        overflow: 'hidden',
        padding: '15px'
    }
})